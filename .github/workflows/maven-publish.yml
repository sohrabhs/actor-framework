name: CI/CD Maven Publish

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    permissions:
      contents: read # You no longer need 'packages: write' for Maven Central

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'
          # Maps to the <publishingServerId>central</publishingServerId> in your POM
          server-id: central
          server-username: CENTRAL_USERNAME
          server-password: CENTRAL_PASSWORD
          # Enables GPG signing configuration in settings.xml
          gpg-private-key: ${{ secrets.GPG_PRIVATE_KEY }}
          gpg-passphrase: GPG_PASSPHRASE

      - name: Publish to Maven Central
        # Explicitly passing the passphrase flag tells GPG not to open a UI prompt
        run: mvn -B deploy -DskipTests -Dgpg.passphrase="$GPG_PASSPHRASE"
        env:
          CENTRAL_USERNAME: ${{ secrets.CENTRAL_USERNAME }}
          CENTRAL_PASSWORD: ${{ secrets.CENTRAL_PASSWORD }}
          # Used by setup-java
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
          # Used natively by maven-gpg-plugin 3.0.0+
          MAVEN_GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}